Drivers are for Windows machines. 

To run the acceptance tests, make sure you have Chrome, Edge, and Firefox installed. Then navigate in the terminal to the selenium folder. Finally, run the TestSite.py file with Python3, sit back and wait about ten minutes, and check the results. 